receivers:
  otlp:
    protocols:
      grpc:

exporters:
  awsxray:
  logging:
    loglevel: debug

service:
  pipelines:
    traces:
      exporters:
        - awsxray
        - logging
      receivers:
        - otlp
  telemetry:
    logs:
      level: debug
